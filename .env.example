# Token z Hugging Face - Wymaga uzupełnienia!
HUGGING_FACE_HUB_TOKEN=
# Włącza/wyłącza szybki transfer (1 = włączony)
HF_HUB_ENABLE_HF_TRANSFER=1

# --- Konfiguracja Modelu ---
# Pełna nazwa modelu z Hugging Face
MODEL_NAME=microsoft/Phi-4-mini-instruct

# Włącza/wyłącza kwantyzację 4-bitową (1 = włączona, 0 = wyłączona)
LOAD_IN_4BIT=1

# Użyj szybkiego tokenizera, jeśli jest dostępny (1 = tak, 0 = nie)
USE_FAST_TOKENIZER=1

# Pozwala na uruchamianie kodu z repozytorium modelu (1 = tak, 0 = nie)
# UWAGA: Wymagane dla wielu nowych modeli (np. Phi, Nemotron).
# Ustaw na 0 dla modeli, które tego nie wymagają, aby zwiększyć bezpieczeństwo.
TRUST_REMOTE_CODE=0

# --- Konfiguracja Generowania ---
# Maksymalna liczba nowych tokenów do wygenerowania
MAX_GENERATION_TOKENS=256

# Systemowy prompt, który określa "osobowość" bota
SYSTEM_PROMPT="Jesteś pomocnym asystentem o nazwie Bielik. Odpowiadaj zawsze po polsku, w sposób zwięzły i precyzyjny."


# --- Opcjonalne (dla zaawansowanych) - można usunąć, kod ma domyślne ---
BNB_4BIT_TYPE=nf4
BNB_4BIT_COMPUTE=bfloat16
FP_DTYPE=bfloat16